[build]
  command = "yarn build"
  publish = "dist" # Use "build" if you're using Create React App instead of Vite

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18" # Or whichever version you're using